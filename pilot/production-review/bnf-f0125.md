# Production review: bnf-f0125

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Column 1 has 47 lines and column 2 has 47 lines. The two printed running headers, page `57.`, signature `P`, and catchword `Co` are recorded. An external line-by-line review confirmed column 1 and exposed a column-2 lineation error: `(midade.` is displaced at the far right of the `mo tinta pera dourar.` physical row, not printed as a separate line. The canonical lineation has been rebased and a resumed external review of regenerated column 2 is pending. Status is `scan_confirmed`; human review is pending.
