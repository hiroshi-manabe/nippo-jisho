# Production review: bnf-f0127

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Column 1 has 47 lines and column 2 has 48 lines. The mismatched printed running headers, page `58.`, and signature `P 2` are recorded. The far-right `ji-` below the signature line is preserved as the final physical text line rather than treated as a catchword. On 2026-08-29, every line crop was remeasured against the native scan and checked line by line, including a separate crop for that displaced line. A separate text pass compared the scan with recurring project error patterns and used NINJAL headwords only to locate possible discrepancies. Status is `scan_confirmed`; human review is pending.
