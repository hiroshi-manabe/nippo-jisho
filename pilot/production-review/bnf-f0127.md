# Production review: bnf-f0127

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 body lines. The mismatched printed running headers, page `58.`, signature `P 2`, and far-right catchword `ji-` below the signature line are recorded. The catchword anticipates f128's opening `jigataxi`, just as f126's `Côtei` anticipates this page's opening `Contei`; it is therefore page furniture rather than an additional body line. On 2026-08-29, every body-line crop was remeasured against the native scan and checked line by line. A separate text pass compared the scan with recurring project error patterns and used NINJAL headwords only to locate possible discrepancies. Status is `scan_confirmed`; human review is pending.
