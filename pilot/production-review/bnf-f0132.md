# Production review: bnf-f0132

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 lines. Both running headers and catchword `Côxi.` are recorded; this verso has no printed page number or signature. On 2026-08-29, every body-line crop was remeasured from the native scan and checked individually; the inherited lower-column drift was removed. A separate scan-led text pass corrected visible `ǒ/ô` distinctions, with NINJAL headwords used only as diagnostic locators. Status is `scan_confirmed`; human review is pending.
