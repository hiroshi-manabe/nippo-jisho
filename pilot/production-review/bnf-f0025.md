# Production review: bnf-f0025

## State

The page was transcribed directly from the Gallica master and completed through the ordinary forward, token-alignment, Japanese, Portuguese, glyph, mark, line-edge, coverage, reverse-order, and fresh-sweep checks. The normal checkpoint was frozen at 2026-08-02 22:16 JST, before the separate marginal audit began. It is `scan_confirmed`, has reviewed line geometry, and awaits human review.

The ordinary passes corrected the visual draft before the checkpoint, including Japanese/context recoveries `Fucacu`, `cumoni ſaqende farauatauo tatçu`, `Aiyome`, and repeated `Ajirocaqe`; the visibly unmarked `Aizo` despite NINJAL's normalized `Aizô`; Portuguese and spacing readings `natea`, `Dar oſinal`, and context-supported `Item`; and three absent line-end division marks after `cou` and `dal` and within other continuations. The same edge pass separately retained the visible italic division marks after `con`, `car`, `cida`, `por`, `ca`, `co`, `Caz`, and `Fal`. NINJAL's headword list was consulted only after the independent visual draft as a coverage and suspicion prompt; the enlarged scan decided every accepted form.

The page contains 47 physical lines in column 1, 37 before and 8 after the internal `A ANTES DO M.` heading in column 2, both running headers, printed page number `7.`, internal heading, signature `B 3`, and catchword `Ama.`. The master SHA-256 was independently rechecked as `39e22258f5963514b01d5e99db46f27b86705fd946becf1fc55394a33656120a`. Both line-geometry contact sheets were visually reviewed before the checkpoint.

## 30–60 marginal-discovery audit

The independent audit began at 2026-08-02 22:16 JST. At 22:19 a mark-isolation pass corrected `vreǒ` to `vreô` on `c1-l014`: the enlarged mark is a peaked circumflex, contrasting directly with the open-bottomed caron on the same line's `Aixǔ`. At 22:36 a tight base-letter comparison corrected semantically expected `Fucacu` to the visibly anomalous `Fucaqu` on `c1-l015`; the closed bowl and full descender are unambiguously `q`, while the following `aigiacu` supplies a same-line `c` comparator. At 22:38 side-by-side isolation of the two apparent `Ajirocaqe` occurrences restored the second to printed `Ajuocaqe` on `c2a-l036`: unlike the complete `jiro` sequence on `c2a-l025`, the later word visibly has only `juo`. This source anomaly also explains why the headword list indexes only the earlier form. At 22:43 a repeated-word allograph comparison corrected the two normalized `atraueſſão` readings independently: `c2a-l010` has a single long `ſ` (`atraueſão`), while `c2a-l015` visibly uses cedilla `ç` (`atraueção`). The same-page `ſenão` and other cedilla types confirm the distinct shapes. At 22:48 a Portuguese spelling sweep restored printed `printuras` in place of modern-biased `pinturas` on `c1-l035`; the extra `r` is complete and dark in enlargement, and an independent OCR token had also flagged it for visual adjudication. At 22:52 an abbreviation check first restored the visible tilde in the second abbreviation on `c2a-l031`. At 23:12 a second diacritic sweep restored printed `Poëtas` in place of normalized `Poetas` on `c1-l027`; two discrete dots over `e` are unmistakable in the master. At 23:14 a historical-spelling check restored printed `baxo` in place of modern `baixo` on `c2a-l014`; enlargement shows `b-a-x-o` with no intervening `i`. At 23:19 a Japanese-semantic pass corrected `Amano tara` to printed `Amano fara` on `c2b-l007`–`008`: the enlarged initial is `f`, 天の原 gives the exact gloss “sky,” and the independently indexed full headword on the following page corroborates the reading. At 23:30 an anomaly-focused vowel comparison corrected semantically expected `Amanogaua` to printed `Amanegaua` on `c2b-l008`: the disputed character is an open `e` with an internal stroke, contrasting with the closed `o` in `Amano` immediately above, and independent OCR also read `e`. At 23:44 a second anomaly-focused base-letter comparison restored `cercido` in place of expected `cercado` on `c2a-l030`: the disputed glyph is a narrow dotted `i`, unlike the page's italic `a`, and independent OCR produced the same reading. At 00:08 a direct comparison of the two abbreviated forms corrected that second reading from `Itẽ` to literal `Itẽm`: unlike the complete `Itẽ` on `c2a-l026`, `c2a-l031` has a faint `e` and superscript tilde followed by a separate, intact `m`. At 00:46 a conjugational-assumption check corrected expected `atta` to visibly printed `arta` on `c1-l003`: the first consonant is a short `r`, contrasting with both tall crossed `t` sorts in the genuine `atta` on `c2a-l002`; two independent OCR passes independently flagged the same anomaly. This final retained finding took about 38 minutes after the preceding success, so the page stopped immediately under the rule's over-30-minute slow-success clause at 2026-08-03 00:46 JST.
