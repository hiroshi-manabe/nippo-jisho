# Production review: bnf-f0150

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 text lines. Both `D ANTES DO A.` running headers and divided catchword `Dai-` are recorded. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

The later exact-crop semantic audit found cumulative lower-column drift. Individually measured centres from `c1-l035` through `c1-l047` now keep the `Daifi` through final `couſas moraes` sequence aligned and readable. Column 2 was already aligned. The content audit confirmed the headword inventory and diacritics against the scan, using NINJAL only to flag candidates. It restored upright Roman type to embedded Japanese `Cami`, `Cunebus`, and `Xaca`, and correctly italicized the Portuguese phrase following Roman `Votodo Cugue`.
