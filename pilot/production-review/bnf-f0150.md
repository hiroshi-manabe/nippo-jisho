# Production review: bnf-f0150

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 text lines. Both `D ANTES DO A.` running headers and divided catchword `Dai-` are recorded. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

The later exact-crop semantic audit was superseded on 2026-08-30 by a fail-closed line-by-line recalibration. All 94 body lines now have scan-measured centres, replacing the former mixture of interpolation and lower-column overrides. Every regenerated card was reopened at review scale and accepted only when its assigned text was independently readable as the primary line. Both column bounds were widened past the printed rules so their right edges are evidential rather than assumed. The content audit confirmed the headword inventory and diacritics against the scan, using NINJAL only to flag candidates. It restored upright Roman type to embedded Japanese `Cami`, `Cunebus`, and `Xaca`, and correctly italicized the Portuguese phrase following Roman `Votodo Cugue`.
