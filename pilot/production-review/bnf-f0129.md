# Production review: bnf-f0129

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 lines. Both running headers, page `59.`, signature `P 3`, and catchword `Cota.` are recorded. On 2026-08-29, every line crop was remeasured against the native scan. This exposed a one-physical-row displacement across all of column 2: the missing top row was restored to `c2-l001`, all following IDs were realigned, and the page furniture formerly occupying `c2-l047` was removed from the body sequence. A separate scan-led text pass corrected the concentrated `ǒ/ô` transcription errors; NINJAL headwords were used only as diagnostic locators. Status is `scan_confirmed`; human review is pending.
