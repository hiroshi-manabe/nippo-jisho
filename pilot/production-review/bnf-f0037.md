# Production review: bnf-f0037

## State

This page received the normal bounded direct-scan workflow: physical coverage, bilingual parsing, individual glyph and mark checks, spacing and line-edge review, reverse reading, and a fresh full-page sweep. NINJAL was used only as a post-draft diagnostic list.

The page contains 47 text lines in each column, both running headers, printed page number `13.`, signature `D`, and catchword `Aſan-`. The scan, unlike the normalized headword data, consistently prints medial long `ſ` in the `Aſa-` headwords; these forms were restored occurrence by occurrence. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`.

GitHub Issue #25 supplied 25 human-review corrections, all independently supported by enlarged scan inspection and applied exactly. Besides textual and spacing corrections, the Issue confirmed roman `Dairi`, two roman `Cha` occurrences, six italic `P.` label spans, and two roman `P.` labels. This local variation is preserved rather than forcing the label into one typeface globally.
