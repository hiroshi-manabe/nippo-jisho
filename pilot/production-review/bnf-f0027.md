# Production review: bnf-f0027

## State

The page was transcribed directly from the Gallica master and completed through the ordinary forward, OCR-token alignment, Japanese, Portuguese, glyph, sibilant-count, mark, spacing, line-edge, coverage, reverse-order, and fresh-sweep checks. The normal checkpoint was frozen at 2026-08-03 04:52 JST, before the separate marginal audit began. It is `scan_confirmed`, has reviewed line geometry, and awaits human review.

The ordinary passes retained source anomalies rather than repairing them from lexical expectation. Same-word and same-page comparisons established printed `niono` after expected `Saxidega maxii mono`, anomalous `Amaſotoqiga ſuru` under headword `Amaſoſoqi`, singular `traueſo`, joined `deſtatura`, `porſer`, `Solque`, `Oque`, and `doceo`, missing-vowel `Mlheres`, and `Foçi-` with a cedilla. They also corrected the visual draft to `bacharel`, abbreviated `Homẽ`, and punctuation `meiças. &`. The later marginal audit reversed an expectation-driven `Bicho` reading to printed `Bilho`, found the repeated printed `iora` anomaly, classified mixed sibilant pairs, and recovered three comma joins. NINJAL's headword list was consulted only after the independent visual draft as a coverage and suspicion prompt: all 37 indexed entries are represented, while the enlarged scan decided every accepted form and disagreement.

The page contains 47 physical text lines in each column, both running headers, printed page number `8.`, and no signature. The bottom-right `metem` is a catchword rather than a 48th text line: f28 begins `metem hũas argolas pera aleuantar a panella`, continuing f27's `por onde`. That adjacent-page check also exposed and corrected the analogous f26 `do` catchword classification after f26's own audit had stopped. The f27 master SHA-256 was independently rechecked as `03ad106455ae3646398b04d723500f62bf1cfcddee4e2aba6f74fae7488899ce`. Both line-geometry contact sheets were visually reviewed before the checkpoint.

## Human correction Issue 15

Issue [#15](https://github.com/hiroshi-manabe/nippo-jisho/issues/15) was reconciled under the linguistic-first Issue procedure and explicit human-override policy on 2026-08-13. Thirteen submitted line items were accepted in the initial adjudication:

| Line | Previous text or styling | Accepted text or styling |
| --- | --- | --- |
| `c1-l003`–`c1-l004` | `Saxidega maxii` / `niono` | `Saxidegamaxii` / `mono` |
| `c1-l011` | `ſignificações` | `ſignificaçoẽs` |
| `c1-l025` | `vnia` | `vma` |
| `c1-l027` | `çalo` | `çado` |
| `c1-l039` | `Solque` | `Sol que` |
| `c1-l045` | `Amaſſaye`; italic `Adu.` | `Amaſsaye`; roman `Adu.` |
| `c2-l014` | `Tequuo` | `Teqiuo` |
| `c2-l033` | `meiças` | `meigas` |
| `c2-l034` | `cães` | `caẽs` |
| `c2-l039` | `eſtà` | `eſta` |
| `c2-l041` | `Romãa` | `Romaã` |
| `c2-l046` | `alguãs` | `algũas` |

The remaining six line items were then re-inspected by the human reviewer and accepted as explicit visual overrides, with two clarifications to the literal Issue payload:

| Line | Previously retained | Human-adjudicated text | Resolution |
| --- | --- | --- | --- |
| `c1-l014`–`015` | `Paſsante` | `Paſſante` | The Issue's three-s strings were obvious typing/copying errors; the intended expansion of the fused glyph was `ſſ`. Original comma spacing remains unchanged. |
| `c1-l032` | `Peſsoa` | `Peſſoa` | Same fused-glyph decision; the Issue's three-s string was a copying error. |
| `c2-l005` | `Amatçumi iora … Amatçu iora` | `Amatçumi ſora … Amatçu ſora` | Human inspection identifies damaged/incomplete long `ſ`; Japanese *misora/sora* and the `Vôzora` equivalence corroborate it. |
| `c2-l012` | `Mlheres moradoras doceo` | `Molheres moradoras doceo` | Human inspection sees the `o` after `M`; ambiguous `doceo` spacing remains literal and unchanged. |
| `c2-l023` | `ou criado` | `on criado` | Retained by explicit human visual override despite the linguistically expected Portuguese conjunction `ou`. |

All 19 submitted line items are therefore resolved and Issue #15 can be closed. The override record deliberately preserves the distinction between visual adjudication and linguistic expectation, especially for anomalous printed `on`.

## 30–60 marginal-discovery audit

The independent audit began at 2026-08-03 04:52 JST. The next-finding clock starts from this time; only retained, certain, scan-supported corrections reset it. Rejected or unresolved candidates do not reset the clock.

- 2026-08-03 04:59 JST — retained `Paſſante` → `Paſsante` in c1-l014 and c1-l015. Enlarged scan comparison shows a long `ſ` followed by short `s` in both occurrences; the correction was found within 30 minutes, so the next-finding clock reset.
- 2026-08-03 05:00 JST — retained `Peſſoa` → `Peſsoa` in c1-l032. The enlarged scan distinctly shows long `ſ` followed by short `s`; the correction was found within 30 minutes, so the next-finding clock reset.
- 2026-08-03 05:04 JST — retained expected `Amatçumi ſora. l, Amatçuſora.` → printed `Amatçumi iora. l, Amatçu iora.` in c2-l005. Both disputed initials are dotted `i` forms and contrast directly with the unmistakable long `ſ` in `Amatçuſora` on c2-l007. NINJAL's linguistically expected `sora` reading is useful evidence for a likely printing anomaly, but Level 1 preserves the two visible `iora` forms. The correction was found within 30 minutes, so the next-finding clock reset.
- 2026-08-03 05:28 JST — retained `Bicho` → printed `Bilho` in c1-l007. A tighter six-line reverse pass shows a full-height `l` between `i` and `h`; the initial is the page's `B` form, not the visibly different italic `M`. The likely lexical pull of intended `Milho de ſaude` had obscured a printed anomaly during the ordinary pass. The correction was found within 30 minutes, so the next-finding clock reset.
- 2026-08-03 05:33 JST — retained `comprido, ou` → printed `comprido,ou` in c1-l027. The comma and following `o` have only their local intertype gap, in direct contrast with the clear post-comma word space in nearby `deſenfreado, ou`; the correction was found within 30 minutes, so the next-finding clock reset.
- 2026-08-03 06:09 JST — retained spaced `Paſsante, ou` and `annos, ou` → printed `Paſsante,ou` and `annos,ou` in c1-l014–l015. Tight nearest-neighbor enlargement and ink-component comparison confirm that each comma is set directly against the following `o`; the finding took more than 30 minutes after the preceding retained correction, so the page stopped immediately under the slow-success branch of the 30–60 rule.

The marginal audit stopped at 06:09 JST by slow success. It retained six finding groups (eight corrected occurrences) after the frozen checkpoint; unresolved or rejected candidates did not reset the clock.
