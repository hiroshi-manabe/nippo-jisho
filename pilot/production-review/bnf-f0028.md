# Production review: bnf-f0028

## State

This page was transcribed directly from the Gallica master with the normal bounded workflow: physical-line coverage, Japanese and Portuguese context, confusable glyphs, sibilants, marks, spacing, line edges, reverse reading, and a final fresh sweep. NINJAL headwords were used only after the visual draft to check coverage and suspicious forms. The scan remains authoritative.

The record contains 47 text lines in each column, both running headers, printed page number `9.`, and the catchword `A AN-`. The preceding `metem` continuation and following section catchword were checked across page boundaries. Targeted Japanese reinspection corrected shape-only drafts to printed `tçuyi`, `Amey ſameto`, `Ameyaſameto`, `votçu`, and `ſuqu`; the unexpected first form is retained rather than normalized to expected *tçuyu*. Both line-geometry contact sheets were visually reviewed. Status is `scan_confirmed`; independent human review is still pending.

## Human correction Issue 16

Issue [#16](https://github.com/hiroshi-manabe/nippo-jisho/issues/16) was adjudicated under the linguistic-first Issue procedure and explicit human-override policy on 2026-08-13. All 27 submitted line items were ultimately accepted, sometimes with human-confirmed corrections to the submitted replacement rather than mechanical application:

- Japanese context and scan detail establish `fajimatte`, `vgocaſu`, `Amiuo fiqu`, `Amiuo voroſu`, `Amiuo vuçu`, `Amiuo tçuq cabuſuru`, `Tçuqiami`, and `Qiǒuo`.
- Human observations of faint or damaged type were accepted as `Ameyaſameto` on `c1-l036` and the visibly spaced anomalous `Ameyaſ meto` on `c1-l037`.
- The abbreviated Portuguese on `c1-l043` is encoded as `q̃ſe`, preserving both the tilde and the printed absence of a word space. `Qujís` on `c2-l006` is roman type within the italic gloss.
- The scan-supported Portuguese changes include `panela`, `Ceo,ou`, `agoamel`/`agoame`, `alfeloa`, `domar`, `pexeſinho`, `camaroẽs`, `janelas`, `Cançellas`, `Teçer`, and `a quel-`.
- `c2-l033` reads `alguã` with the mark over final `a`; the absence of a line-end hyphen is also retained.

The proposal `c1-l009` `tçuyi` → `tçuyu` was initially marked **Human re-check required** because machine inspection treated a small mark as an `i` dot. The human reviewer sees an unambiguous printed `u` and explicitly overrode that judgment; Japanese *tsuyu*, the proverb's reference to dew, and multiple dictionary comparators independently corroborate `tçuyu`. The machine-qualified `Tçuqiami` in place of submitted `Tçuquiami` was also explicitly checked and confirmed by the human reviewer. Issue #16 is therefore fully resolved.
