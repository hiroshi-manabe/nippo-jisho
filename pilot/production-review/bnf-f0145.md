# Production review: bnf-f0145

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 lines. Both running headers, page `67.`, signature `R 3`, and the printed catchword `Auer` are recorded. The displaced `Curô.` and `Curoſa.` remain distinct physical lines in column 1. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

On 2026-08-30, every body-line crop was reopened with its assigned transcription and checked for readable vertical and horizontal coverage. Both lower-column calibrations were tightened, and both columns were widened where necessary to retain the printed rules. A second exact-card audit raised `Curǒ.`, `Curoſa.`, and the final `Curozatǒ. Iugid.` card slightly so their glyph tops are not clipped. The scan restored `chã-`, corrected `deſcalçar` to `deſcaſcar`, and restored the missing `t` in `Artelhos`.
