# Production review: bnf-f0135

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

On 2026-08-30, the isolated bottom-right `riuo` was reclassified from body line `c2-l048` to catchword `cw-l001`. It duplicates the opening `riuo` on f136 and is spatially separated from the final body line, while the printed `Q 2` remains the signature.

Column 1 has 35 lines before and ten after the internal `C ANTES DO V.` heading; column 2 has 48 lines. Both running headers, page `62.`, and signature `Q 2` are recorded. On 2026-08-29, every body-line crop was remeasured from the native scan and checked individually. This exposed and corrected a uniform one-physical-row displacement in column 2: `c2-l001` is the continuation below the running header, not the header itself. A separate scan-led text pass corrected visible vowel marks and `Cozzumi`, with NINJAL headwords used only as diagnostic locators. Status is `scan_confirmed`; human review is pending.
