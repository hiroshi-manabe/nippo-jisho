# Production review: bnf-f0024

## State

The page was transcribed directly from the Gallica master and completed through the ordinary forward, token-alignment, Japanese, Portuguese, glyph, mark, line-edge, coverage, reverse-order, and fresh-sweep checks. The normal checkpoint was frozen at 2026-08-02 18:37 JST, before the separate marginal audit began. It is `scan_confirmed`, has reviewed line geometry, and awaits human review.

The ordinary checks corrected the initial visual draft before the checkpoint, including `neuoa`, source-anomalous `Aitçunori, ru, etta`, the circumflexes in `Aitanomi, u, ôda` and `Aitotonoi, ô, ôta`, `qiuamite`, `Eſtimar,`, `fullas`, and `tratamẽto`. NINJAL's headword list was consulted only after the independent visual draft, as a coverage and suspicion prompt; the scan decided every accepted form, including disagreements.

## 30–60 marginal-discovery audit

The independent audit began at 2026-08-02 18:37 JST. At 18:41 it retained its first correction, restoring the visibly anomalous reference `Vtçuxi ſu,` on `c2-l012` in place of the regularized `Vtçuxi, ſu.`. At 18:49 it retained the printed `ǔ` in `Aiuqegai, ǔ, ǒta`, where both Japanese morphology and NINJAL encourage the regularized `ǒ`; the enlarged scan clearly shows an open-topped `u` base beside the circular `o` in `ǒta`. At 18:56 a fresh semantic-and-glyph sweep corrected OCR-biased `criado` to visibly abbreviated `brãdo` in `Tratar a hum brãdo ou affabilmente`; the initial bowl, following `r`, tilde, and sense all agree. At 19:02 direct comparison of the two marks in the opening line retained source-anomalous `aiſôraxij aixirǒ`: the first mark is a peaked circumflex, while the second is a clear caron, despite the `Aiſǒraxij` headword on f23. The adjacent paired dots of final `ij` were not misclassified as another mark. At 19:06 a high-enlargement mark pass recovered the faint but complete caron over `e` in `Aitaixite mělu`, correcting both the initial circumflex hypothesis and the checkpoint's unmarked reading. At 19:10 a morphology-led enlargement restored the faint middle letters in `qiuamarite` (極まりて), which several OCR views had collapsed to `qiuamite`; all ten letters are supported by the scan. At 19:21 comparison with the project's verified `Ienxùs` grave specimen resolved an earlier hesitation in favor of the literal `Aitarai, ǒ, òta. Vide Tarai, o.`: the past form has the same single descending stroke, and the reference is visibly unmarked. At 19:22 the same literal mark sweep removed an expected but absent caron from `Aitatacai, ǒ, ota`; the following `Tatacai, ǒ` reference supplies a same-line contrast with a clearly printed mark. At 19:27 the occurrence-by-occurrence comparison found the same clean grave stroke in `Aitagai, ǒ, òta`, contrasting with the full caron in following `Tagai, ǒ`. At 19:49 an edge-specific line-break pass removed the supplied hyphen after `af` in `c1-l001`: the enlarged master shows the terminal italic `f` and clear paper before the column rule, but no printed hyphen; the following line nevertheless begins `fabilmente`. At 19:55 the same pass removed another supplied hyphen after `fron` in `c1-l019`: the master again shows blank paper between the final letter and the rule, although `te` begins the continuation line. At 19:58 it removed the supplied hyphen after `cre` in `c2-l004`; the same-page italic `=`-shaped line-end hyphens are plainly visible after `al`, `flo`, `da`, and `pro`, whereas no such type follows `cre` before `cerão`. At 19:59 it likewise removed the supplied hyphen after roman `xe` in `c2-l045`: the preceding italic line visibly ends in `com=` but this line ends with bare `xe` before `muru`. At 20:15 a long-/short-s and multiplicity pass corrected normalized `peſſoa` to the visibly single-s `peſoa` in `c1-l007`; the enlarged word contains `p-e-ſ-o-a`, with no second type between the long `ſ` and `o`. At 20:19 it corrected `neceſſidade` to `neceſsidade` in `c2-l030`: here the scan does contain two s-types, but the second is the compact short `s`, not another long `ſ`. At 20:36 a punctuation-isolation pass restored the small but saturated baseline period in `A. xirouo` on `c2-l045`; its placement and ink match punctuation rather than the faint show-through and paper specks nearby, despite the resulting unusual Japanese phrase. At 21:30 a full-page spacing sweep found the next certain correction: `Padraſto,ou` on `c2-l043` is visibly set without a space after the comma. Because this came 54 minutes after the preceding retained finding, the page stopped immediately under the slow-success clause. Morphology and NINJAL remained alarms, not permission to normalize the printed marks; damaged `Aitoburai`, and scan-confirmed `mělu`, `vmagia`, and `vonoxi`, were retained without resetting the clock.
