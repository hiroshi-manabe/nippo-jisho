# Production review: bnf-f0126

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 48 lines. The two printed running headers and catchword `Côtei` are recorded; this verso has no printed page number or signature. Both geometry contact sheets were visually reviewed with page-specific lower bounds for the final lines. Status is `scan_confirmed`; human review is pending.
