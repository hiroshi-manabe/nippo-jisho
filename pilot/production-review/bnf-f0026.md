# Production review: bnf-f0026

## State

The page was transcribed directly from the Gallica master and completed through the ordinary forward, OCR-token alignment, Japanese, Portuguese, glyph, mark, line-edge, coverage, reverse-order, and fresh-sweep checks. The normal checkpoint was frozen at 2026-08-03 01:22 JST, before the separate marginal audit began. It is `scan_confirmed`, has reviewed line geometry, and awaits human review.

The ordinary passes retained literal typography and source anomalies rather than regularizing them. Enlarged same-page comparisons established printed `Amatiyori` against lexically expected `Amabiyori`, `Amagayeiu` against expected `Amagayeru`, and `Amano tebeco` against expected `Amano toboco`. They also restored the circumflex in variant `Amaneô`, joined printed `Vialactea`, `ofun-`, and `domar`, and preserved the visibly absent division mark across `bo` / `ião`. The line-edge pass separately retained printed division marks after `e`, `Ama`, `tẽ`, `ſom`, `al`, `pa`, `li`, `Figaxo`, `Obi`, `Izana`, `fun`, `ſe`, `na`, `on`, and `mun`. NINJAL's headword list was consulted only after the independent visual draft as a coverage and suspicion prompt; the enlarged scan decided every accepted form and disagreement.

The page contains 47 physical text lines in each column, both running headers, and no printed page number or signature. The tiny bottom-right `do` is a catchword: the following page begins `do às eſcuras`, completing the column's printed `mun-` as `mundo` before continuing the sentence. This classification was corrected only when f27 was opened after f26's independent audit had stopped; the adjacent-page evidence did not reopen the marginal-discovery clock. The master SHA-256 was independently rechecked as `5be6f0892651198f024e9c17c7ef080c8d19dafd22c7092518380d97a0847f8a`. Both line-geometry contact sheets were visually reviewed before the checkpoint.

## Human correction Issue 14

Issue [#14](https://github.com/hiroshi-manabe/nippo-jisho/issues/14) was partially reconciled with enlarged scan evidence on 2026-08-13. Fifteen lines contain accepted corrections, including four typeface corrections and a qualified correction on `c1-l027`:

| Line | Previous text or styling | Scan-confirmed text or styling |
| --- | --- | --- |
| `c1-l006` | `deſfaz` | `desfaz` |
| `c1-l018` | italic `Cha` | roman `Cha` |
| `c1-l027` | `Armar` | `Amarrar` (the submitted `galamiuo` → `garamiuo` change was not verified) |
| `c1-l031` | `cae` | `caye` |
| `c1-l032` | `cerração` | `çerração` |
| `c1-l044` | `Romani` | `Romam` |
| `c1-l045` | `Viujiuo` | `Vſujiuo` |
| `c2-l004` | `embarcações` | `embarcaçoẽs` |
| `c2-l018` | `vniuersal` | `vniuerſal` |
| `c2-l026`–`c2-l027` | italic `Izana-mi` and `Izanagui` | roman `Izana-mi` and `Izanagui` |
| `c2-l029` | `gerou` | `gèrou` |
| `c2-l039`–`c2-l040` | italic `Izanami` and `Izanagui` | roman `Izanami` and `Izanagui` |
| `c2-l047` | italic `Tenxôdayin` | roman `Tenxôdaijin` |

Fifteen submitted line items remain provisionally rejected and require human re-check, so the Issue remains open. Enlarged same-page comparison supports the existing literal anomalies `bichintos`, `Amatiyori`, `qeiu`, `iu`, `beirasdoteihado`, `delgaça`, `galamiuo`, `Amagayeiu`, `ſonzear`, `alguã`, `Amano tebeco`, and `partizuna`; it also supports the existing `& e-`, spaced `pera a chuua`, and `Amajiuoro uo`. Several of these are linguistically unexpected, but Level 1 records the printed witness rather than silently normalizing it.

## 30–60 marginal-discovery audit

The independent audit began at 2026-08-03 01:22 JST. At 01:24 a morphology-directed enlargement corrected `voniomuite` to printed `vomomuite` on `c2-l014`: the disputed sequence visibly contains `m-o-m`, and the Japanese construction independently identifies the expected span without overriding the type. At 01:26 a dotted-letter comparison corrected regularized `qeru` to printed `qeiu` on `c1-l014`: the disputed letter has a clear dot and no `r` shoulder, while the same line's `Amacaqeri` supplies the contrast. At 01:31 the same explicit-dot test corrected expected `ru` to printed `iu` in `Amacudari, iu, atta` on `c1-l019`; the intact `r` in the immediately preceding `-dari` supplies a same-line comparator. At 01:35 continuation-line enlargement restored the single printed sequence `beirasdoteihado` on `c1-l021` in place of normalized `beiras do telhado`: there are no internal spaces, and the disputed vowel is a dotted `i`, not Portuguese `l`. At 01:38 a historical-spelling comparison corrected modern-biased `ſonjear` to printed `ſonzear` on `c2-l012`; the middle sort matches the page's `z` in `fazer`, not its `j`. At 01:51 a page-wide historical-spelling check corrected modernized `chover` to printed `chouer` at all three occurrences (`c1-l013`, `c2-l002`, and `c2-l007`): each enlarged middle sort is the italic `u` used elsewhere on the page. This was retained as one repeated-pattern finding and reset the clock once. At 02:16 a reverse contact-sheet sweep corrected modern-biased `delgada` to printed `delgaça` on `c1-l024`: enlargement shows a short `ç` with a cedilla below the baseline, not italic `d` with an ascender. At 02:44 a complete diagonal-accent inventory corrected modernized `eſtá` and `pés` to printed `eſtà` and `pès` at both occurrences of each (`c1-l013`, `c1-l026`, `c1-l023`, and `c2-l005`): all four marks are parallel to the secure grave in `à terra` on `c2-l029`, and no acute comparator occurs on the page. This was retained as one page-wide pattern finding and reset the clock once. At 02:59 a Japanese-form challenge followed by a same-font `l/r` comparison corrected expected `garamiuo ſuru` to anomalously printed `galamiuo ſuru` on `c1-l027`: the disputed sort matches the page's printed lowercase `l` marker, unlike the short shouldered `r` in the same line's `ſuru` and nearby `Amacudari` and `Amaguiru`. Four provisional readings were subsequently rejected and did not reset the clock. First, same-font `Amano fara` proves that the basin-like disputed sort in `Ameno farema` is this upright type's lowercase `n`, not `u`. Second, quantitative enlargement of `Decer do ceo` shows a narrow but real word space between `do` and `ceo`, wider than the internal `c–e` gap; the temporary joined reading was therefore restored to the checkpoint form. Third, a high-magnification same-word comparison disproved the modernizing change from printed `bichintos` to expected `bichinhos`: the first disputed ascender is an italic `h`, but the second is visibly the page's crossed `t`, so checkpoint `bichintos` was restored. Fourth, an equal-scale `i/r` montage disproved provisional `cotayuiu`: unlike the detached dots in secure `qeiu` and `Amagayeiu`, the disputed sort has no dot and has the shoulder of upright `r`, so checkpoint `cotayuru` was restored. The next-finding clock was reset only after each retained correction. No further certain error appeared during the 60 minutes following the 02:59 finding, despite reverse-order glyph, punctuation, spacing, mark, line-edge, crop-containment, and frozen-diff checks. The page therefore stopped at the 03:59 JST no-success boundary under the page-independent 30–60 rule; final validation completed immediately afterward.
