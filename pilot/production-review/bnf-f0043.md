# Production review: bnf-f0043

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

The page contains 48 lines in column 1 and 46 in column 2, split around the internal `A ANTES DO X.` heading. It also records both running headers, page `16.`, signature `D 4`, and catchword `reſtea`. The enlarged `AXE` line has an explicit crop override. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.
