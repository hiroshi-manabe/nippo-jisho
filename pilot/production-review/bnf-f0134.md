# Production review: bnf-f0134

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 lines. The anomalous mismatch between the printed `C ANTES DO O.` and `C ANTES DO H.` running headers and catchword `Cozu` are recorded; no page number or signature is printed. On 2026-08-29, every body-line crop was remeasured from the native scan and checked individually, including the unusually low final rows. A separate scan-led text pass corrected visible carons, circumflexes, tildes, and graves, with NINJAL headwords used only as diagnostic locators. Status is `scan_confirmed`; human review is pending.
