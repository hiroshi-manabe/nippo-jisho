# Production review: bnf-f0154

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 text lines. Both `D ANTES DO E.` running headers and the visibly printed catchword `taca?` are recorded. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

The later exact-card audit found that the page's irregular baselines defeated simple interpolation, especially in the lower rows. All 94 line centres were therefore measured from the scan and recorded explicitly; regenerated cards now keep every assigned line readable through `Denju` and `deqi yori maraxi-`, while expanded bounds include both column rules. A second fail-closed card audit on 2026-08-31 confirmed every assignment without further geometry changes. A separate full-page content and typography pass checked every line against the enlarged scan and found no defensible transcription change.
