# Production review: bnf-f0170

## Detailed commented review — 2026-09-05

Completed the current two-pass procedure: an individual English durable note
was written while reading each of the 94 body lines against its scan crop,
with the generated kana hints as a reference. After finishing the commentary,
a fresh pass returned to the beginning and inspected every body line again.
Short continuations and displaced body fragments were included; furniture was
not counted as body commentary. The note-coverage check is 94/94.

Corrected ten rows: `importei` to `imporei`; the shortened source label `Tai.`; `Cabe- / çuo` to `Cobe- / uo`; `Nacatazzuqi` to `Nacarazzuqi`; `ſuritçuguru` to `ſuritçuquru`; `Yonchacama` to `Yonobacama`; `iquiru` to `iquru`; `are-` to `er-` before `uas`; and `bilhas` to `telhas`. The NINJAL headword `Yonobacama` supplied supporting context, not substitute scan evidence. The ship-compartment noun `bilj`, comparison `cainte`, unusual `ycitǒ`, and worn right-end `homeẽs` remain qualified in their notes.

Existing crop geometry and human-review counts were not changed. These notes
record contextual evidence and remaining uncertainty, not a guarantee that the
transcription is error-free or a claim of human approval.

## State

This page received an evidence-weighted generic-AI review of the independent Calamari OCR draft. NINJAL and linguistic context guided meaning-bearing readings; scan-aligned OCR received a presumption for reading-neutral diplomatic details, with the scan remaining authoritative.

Column 1 has 47 text lines and column 2 has 47. Both running headers and catchword `¶ Itẽ,` are recorded. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

An additional Japanese-focused scan pass corrected `ariqeruuo` and restored the printed nasal abbreviation in line-final `Fã-`, which continues as `guini` and therefore represents `Fanguini`.

A subsequent independent bilingual pass joined Portuguese `acarretão` and corrected Japanese `Gorocujùnin` and the base reading of `Chùbude`.

A generated-reading-assisted pass caught that the enlarged mark in `Chǔbude` is specifically a caron, not a grave accent, and corrected the diplomatic transcription accordingly. The remaining Japanese and Portuguese produced no further justified correction.

The current contextual pass and fresh whole-page pass checked every line in both languages. An enlarged comparison showed that the far-right material after `o vinho.` is the displaced printed fragment `(&c.`, not a paragraph sign; it is now encoded explicitly as displaced matter. No further scan-supported change was found.
