# Production review: bnf-f0147

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Column 1 has 47 lines and column 2 has 48 lines. Both running headers and page `68.` are recorded. The final `canuo` fragment is retained as a physical line in column 2. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

The later exact-crop semantic audit found cumulative lower-column drift. Individually measured centres from `c1-l036` through `c1-l047` now keep every assigned row readable through the final `Cutçugayexi` line. Column 2 was already aligned. The accompanying content audit confirmed the headwords against the scan and diagnostic NINJAL sequence, and restored upright Roman type to embedded Japanese `Fune` within the italic Portuguese explanation at `c2-l004`.
