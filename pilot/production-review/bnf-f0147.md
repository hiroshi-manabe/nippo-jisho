# Production review: bnf-f0147

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Column 1 has 47 lines and column 2 has 48 lines. Both running headers and page `68.` are recorded. The final `canuo` fragment is retained as a physical line in column 2. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

An initial exact-crop audit incorrectly accepted column 2 because overlap left fragments of the assigned lines visible; `c2-l045`, for example, was centred on the following `Cuuaxù` row while the assigned `particular direi deuagar.` was clipped at the top. That review claim is superseded. The replacement audit measured and recorded explicit centres for all 47 first-column and all 48 second-column lines, widened both columns beyond their printed rules, regenerated every browser-facing card, and required the complete assigned line to be independently readable as the primary line. All 95 cards now pass that test, including `c2-l045` and the displaced final `canuo`. The accompanying content audit confirmed the headwords against the scan and diagnostic NINJAL sequence, and restored upright Roman type to embedded Japanese `Fune` within the italic Portuguese explanation at `c2-l004`.
