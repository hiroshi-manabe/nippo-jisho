# Production review: bnf-f0031

## State

This page received the normal bounded visual, bilingual-context, glyph, diacritic, spacing, reverse, and fresh-sweep checks. The later NINJAL comparison was limited to coverage and suspicion generation.

Column 2 contains separate internal P and Q sections. Their headings are furniture, while `APPare` and `AQE` begin with enlarged initials spanning two physical lines. The section transitions and full-glyph crops were adjusted by contact-sheet inspection rather than uniform interpolation. The page contains 90 text lines, both running headers, printed page number `10.`, signature `C 2`, and catchword `Deſ-`. Status is `scan_confirmed`.

GitHub issue #19 supplied eight corrections, all now applied. Seven were independently confirmed from the scan and linguistic context. For `c1-l008`, the human reviewer established that `propria` ends without punctuation and that the apparent mark belongs to the line below; this overrides the machine visual assessment.
