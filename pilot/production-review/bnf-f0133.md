# Production review: bnf-f0133

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Column 1 has 48 lines and column 2 has 47 lines. Both running headers, page `61.`, signature `Q`, and catchword `Coxxet` are recorded. On 2026-08-29, every body-line crop was remeasured from the native scan and checked individually; the first column was widened to restore clipped initials and both final-line ranges were lowered. A separate scan-led text pass corrected visible vowel marks and printed `Sẽſualidade`, with NINJAL headwords used only as diagnostic locators. Status is `scan_confirmed`; human review is pending.
