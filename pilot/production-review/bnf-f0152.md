# Production review: bnf-f0152

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 text lines. The mismatched printed running headers—`D ANTES DO B.` over column 1 and `D ANTES DO A.` over column 2—and catchword `Date.` are preserved. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

The later exact-card audit exposed a one-row phase error in both columns, not merely lower-page interpolation drift. On 2026-08-31 all 94 body-line centres were recorded explicitly from the scan and every regenerated card was checked against its assigned transcription through the final `Gentios.` row. Wider horizontal bounds preserve both printed column rules as absence evidence.

On 2026-09-01 the page was transcribed again in a scan-led pass that did not treat the provisional text as an authority. The headword list and line OCR were consulted only after the visual reading, as diagnostics. A complete enlarged-card sweep confirmed that every assigned rectangle contains its physical line and both column rules. The independent pass corrected `Tachi nozoqu`, `Sentençiar`, the comma in `Daqe. i,`, `refolhado`, `vobitataxij`, roman `Tera`, and `O mòr`; it also regularized the type assignment of the printed `S.` token without changing its text. The damaged fourth consonant in line 1 is retained contextually as `Dandã-`: its lower bowl survives weakly, while `Dandãni cataru` supplies the coherent Japanese reading and the printed continuation confirms the division.
