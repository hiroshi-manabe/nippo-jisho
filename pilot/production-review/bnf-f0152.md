# Production review: bnf-f0152

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 text lines. The mismatched printed running headers—`D ANTES DO B.` over column 1 and `D ANTES DO A.` over column 2—and catchword `Date.` are preserved. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`; human review is pending.

The later exact-card audit exposed cumulative interpolation drift near the foot of both columns: the old crops became one physical row late from `c1-l042` and `c2-l039`, and the final second-column card showed the catchword instead of `Gentios.` Scan-derived centres now keep all 94 assigned lines readable through both final body rows, with wider horizontal bounds preserving the column rules as absence evidence. The accompanying enlarged-scan content and typography pass found no defensible transcription change.
