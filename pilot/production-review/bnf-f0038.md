# Production review: bnf-f0038

## State

This page received the normal bounded direct-scan workflow: physical coverage, bilingual parsing, glyph and mark checks, spacing and line-edge review, reverse reading, and a fresh full-page sweep. NINJAL was used only as a post-draft diagnostic list.

The page contains 47 lines in column 1 and 45 in column 2, split around the internal `A ANTES DO T.` heading. Both running headers and catchword `tara` are recorded. The enlarged `ATA` line has an explicit crop override. Both geometry contact sheets were visually reviewed. Status is `scan_confirmed`.

GitHub Issue #26 supplied 33 human-review proposals. Enlarged scan inspection and linguistic review support 31, which have been applied exactly, including roman `Qijis`, roman `Ximo`, roman `i.`, and the printed but linguistically unusual `coqu`. Two proposals remain pending human re-check: `c2b-l007` appears to retain the printed continuation `A-`, while `c2b-l028` appears to retain `preſum-`, consistent with the following `tuoſo`.
