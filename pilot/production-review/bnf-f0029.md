# Production review: bnf-f0029

## State

This page received the normal bounded scan workflow, including separate Japanese, Portuguese, glyph, mark, spacing, reverse-order, and fresh-sweep passes. NINJAL supplied a later coverage prompt only; visible capitalization, long `ſ`, punctuation, and spacing follow the Gallica scan.

The internal `AN` initial spans two physical lines and is explicitly annotated and given a complete-glyph review crop. Japanese-context reinspection restored the clearly printed alias `iuori` from a shape-only draft. The page contains 45 text lines in column 1, 48 in column 2, both running headers, signature `C`, and catchword `fon-`; no printed page number is present. Geometry was visually reviewed. Status is `scan_confirmed` and awaits human review.

## Human correction Issue 17

Issue [#17](https://github.com/hiroshi-manabe/nippo-jisho/issues/17) was partially adjudicated under the linguistic-first Issue procedure on 2026-08-13. Ten of twelve submitted line items were accepted exactly as submitted: `Oesnoroeſte` / `oesnoroeſte`, two absent line-end hyphens, printed anomalous `Couas`, `Interiecão`, `eſſes`, an absent final period, caron `yǒna`, joined `aboca`, and historical `iniuſto`.

Two items remain **Human re-check required**, so the Issue remains open:

- `c2-l007`: submitted short-s `Esfoladura` was not applied because enlargement appears to show a tall long `ſ`, supporting existing `Eſfoladura`.
- `c2-l026`: submitted `algũa` was not applied. The complete tilde-position audit already classifies this occurrence as `alguã`, and tight enlargement places the mark over the final `a`, not `u`.
