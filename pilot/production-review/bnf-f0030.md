# Production review: bnf-f0030

## State

This page was completed with the normal bounded workflow and checked for Japanese morphology, historical Portuguese, individual base letters and marks, exact spacing, line endings, coverage, and reverse-order surprises. NINJAL headwords were consulted after the independent draft as a diagnostic list, not copied.

The page continues `fon-` from f29 and ends with catchword `ou`. It contains 47 text lines in each column and both running headers; there is no printed page number or signature. Both column contact sheets were visually reviewed. Status is `scan_confirmed`; human comparison remains pending.

## Human correction Issue 18

Issue [#18](https://github.com/hiroshi-manabe/nippo-jisho/issues/18) was partially adjudicated under the linguistic-first Issue procedure on 2026-08-13. Twenty-nine exact submitted corrections were accepted, including eight typeface-run corrections whose plain text is unchanged.

Four items remain **Human re-check required**, so the Issue remains open:

- `c1-l004`: the submitted `varellas dos den-` is not an exact scan-supported form. The scan appears compatible with `varellas dos len-`: `varellas` is linguistically and visually preferable to current `varcellas`, but the line-ending word remains visually `len-`, not submitted `den-`. This qualified third form requires human confirmation.
- `c1-l034`: submitted `ſaimin` is linguistically supported by Japanese *saimin* and the scan appears to show a long `ſ`, but the exact submitted line also changes the preceding line-end punctuation/spacing. A qualified form retaining the printed separation must be confirmed rather than silently applied.
- `c2-l006`: the existing `deſcorrer` is positively supported by both the scan and Portuguese; submitted `deſcorrcr` would replace the second `e` with `c`.
- `c2-l034`: Japanese morphology strongly supports existing `annaiuo ſuru`; the printed long `ſ` can resemble `f`, but does not justify submitted `feru`.
