# Production review: bnf-f0141

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 47 lines. Both running headers, page `65.`, signature `R`, and catchword `Cuni-` are recorded. The displaced `i. fabrica.` remains on its printed line and is not duplicated as a separate physical line. Status is `scan_confirmed`; human review is pending.

On 2026-08-30, a browser report exposed a one-row phase error that the earlier contact-sheet review had missed: both columns treated the running header as though it were the first body row, leaving every body crop about 50 source pixels too high. All 94 body-line centres, crops, and context crops were shifted down by 50 pixels. The exact crops were then regenerated from the canonical geometry and inspected beside their assigned transcriptions, from `c1-l001` and `c2-l001` through both final lines; the intended text is now readable in every card. This correction is also why the general workflow now requires a post-write crop-to-transcription semantic audit rather than accepting an orderly contact sheet alone.

The scan review also restored the nasal mark in line-final `Rẽ-` and corrected `Aquitarſe` to printed `Aquietarſe`.
