# Production review: bnf-f0066

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Column 1 has 48 stable line records occupying 47 vertical rows: the displaced `(ceſſores.` fragment is retained once as a far-right record at the same physical height as `mão do Fotoqe.` while logically continuing `ſo-` below. Column 2 has 47 lines. Both running headers and catchword `But-` are recorded. A later external-AI diagnostic exposed the duplicated fragment and the resulting one-row geometry drift; column 1 was then realigned against the scan. Status is `scan_confirmed`; human review is pending.
