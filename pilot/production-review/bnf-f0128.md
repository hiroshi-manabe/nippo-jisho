# Production review: bnf-f0128

## State

This page received the normal bounded direct-scan workflow, with NINJAL used only as a post-draft diagnostic list.

Both columns have 48 lines. The mismatched printed running headers and catchword `Côrô.` are recorded; the opening line completes f127's displaced `ji-`. On 2026-08-29, every line crop was remeasured against the native scan and then checked line by line for readable vertical and horizontal coverage. A separate text pass compared the scan with recurring project error patterns and used NINJAL headwords only to locate possible discrepancies. Status is `scan_confirmed`; human review is pending.
